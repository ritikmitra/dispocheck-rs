/target
Cargo.lock
*.pdb
.DS_Store

# This is a library crate, so Cargo.lock is intentionally not committed
# (it's regenerated by CI / consumers). Remove the line above and commit
# Cargo.lock if you'd prefer to pin dev/CI dependency versions instead.
